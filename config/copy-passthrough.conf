# === GENERATED by bin/gen-passthrough — DO NOT EDIT BY HAND ===
# copy-mode-vi 'type-to-exit': any printable key cancels copy-mode and
# lands the literal char at the live prompt. Regenerate with:
#   bin/gen-passthrough > config/copy-passthrough.conf
# Preserved copy keys (not overridden): V q v y
bind -T copy-mode-vi '!' send -X cancel \; send -l -- '!'
bind -T copy-mode-vi '"' send -X cancel \; send -l -- '"'
bind -T copy-mode-vi '#' send -X cancel \; send -l -- '#'
bind -T copy-mode-vi '$' send -X cancel \; send -l -- '$'
bind -T copy-mode-vi '%' send -X cancel \; send -l -- '%'
bind -T copy-mode-vi '&' send -X cancel \; send -l -- '&'
bind -T copy-mode-vi "'" send -X cancel \; send -l -- "'"
bind -T copy-mode-vi '(' send -X cancel \; send -l -- '('
bind -T copy-mode-vi ')' send -X cancel \; send -l -- ')'
bind -T copy-mode-vi '*' send -X cancel \; send -l -- '*'
bind -T copy-mode-vi '+' send -X cancel \; send -l -- '+'
bind -T copy-mode-vi ',' send -X cancel \; send -l -- ','
bind -T copy-mode-vi '-' send -X cancel \; send -l -- '-'
bind -T copy-mode-vi '.' send -X cancel \; send -l -- '.'
bind -T copy-mode-vi '/' send -X cancel \; send -l -- '/'
bind -T copy-mode-vi '0' send -X cancel \; send -l -- '0'
bind -T copy-mode-vi '1' send -X cancel \; send -l -- '1'
bind -T copy-mode-vi '2' send -X cancel \; send -l -- '2'
bind -T copy-mode-vi '3' send -X cancel \; send -l -- '3'
bind -T copy-mode-vi '4' send -X cancel \; send -l -- '4'
bind -T copy-mode-vi '5' send -X cancel \; send -l -- '5'
bind -T copy-mode-vi '6' send -X cancel \; send -l -- '6'
bind -T copy-mode-vi '7' send -X cancel \; send -l -- '7'
bind -T copy-mode-vi '8' send -X cancel \; send -l -- '8'
bind -T copy-mode-vi '9' send -X cancel \; send -l -- '9'
bind -T copy-mode-vi ':' send -X cancel \; send -l -- ':'
bind -T copy-mode-vi ';' send -X cancel \; send -l -- ';'
bind -T copy-mode-vi '<' send -X cancel \; send -l -- '<'
bind -T copy-mode-vi '=' send -X cancel \; send -l -- '='
bind -T copy-mode-vi '>' send -X cancel \; send -l -- '>'
bind -T copy-mode-vi '?' send -X cancel \; send -l -- '?'
bind -T copy-mode-vi '@' send -X cancel \; send -l -- '@'
bind -T copy-mode-vi 'A' send -X cancel \; send -l -- 'A'
bind -T copy-mode-vi 'B' send -X cancel \; send -l -- 'B'
bind -T copy-mode-vi 'C' send -X cancel \; send -l -- 'C'
bind -T copy-mode-vi 'D' send -X cancel \; send -l -- 'D'
bind -T copy-mode-vi 'E' send -X cancel \; send -l -- 'E'
bind -T copy-mode-vi 'F' send -X cancel \; send -l -- 'F'
bind -T copy-mode-vi 'G' send -X cancel \; send -l -- 'G'
bind -T copy-mode-vi 'H' send -X cancel \; send -l -- 'H'
bind -T copy-mode-vi 'I' send -X cancel \; send -l -- 'I'
bind -T copy-mode-vi 'J' send -X cancel \; send -l -- 'J'
bind -T copy-mode-vi 'K' send -X cancel \; send -l -- 'K'
bind -T copy-mode-vi 'L' send -X cancel \; send -l -- 'L'
bind -T copy-mode-vi 'M' send -X cancel \; send -l -- 'M'
bind -T copy-mode-vi 'N' send -X cancel \; send -l -- 'N'
bind -T copy-mode-vi 'O' send -X cancel \; send -l -- 'O'
bind -T copy-mode-vi 'P' send -X cancel \; send -l -- 'P'
bind -T copy-mode-vi 'Q' send -X cancel \; send -l -- 'Q'
bind -T copy-mode-vi 'R' send -X cancel \; send -l -- 'R'
bind -T copy-mode-vi 'S' send -X cancel \; send -l -- 'S'
bind -T copy-mode-vi 'T' send -X cancel \; send -l -- 'T'
bind -T copy-mode-vi 'U' send -X cancel \; send -l -- 'U'
bind -T copy-mode-vi 'W' send -X cancel \; send -l -- 'W'
bind -T copy-mode-vi 'X' send -X cancel \; send -l -- 'X'
bind -T copy-mode-vi 'Y' send -X cancel \; send -l -- 'Y'
bind -T copy-mode-vi 'Z' send -X cancel \; send -l -- 'Z'
bind -T copy-mode-vi '[' send -X cancel \; send -l -- '['
bind -T copy-mode-vi '\' send -X cancel \; send -l -- '\'
bind -T copy-mode-vi ']' send -X cancel \; send -l -- ']'
bind -T copy-mode-vi '^' send -X cancel \; send -l -- '^'
bind -T copy-mode-vi '_' send -X cancel \; send -l -- '_'
bind -T copy-mode-vi '`' send -X cancel \; send -l -- '`'
bind -T copy-mode-vi 'a' send -X cancel \; send -l -- 'a'
bind -T copy-mode-vi 'b' send -X cancel \; send -l -- 'b'
bind -T copy-mode-vi 'c' send -X cancel \; send -l -- 'c'
bind -T copy-mode-vi 'd' send -X cancel \; send -l -- 'd'
bind -T copy-mode-vi 'e' send -X cancel \; send -l -- 'e'
bind -T copy-mode-vi 'f' send -X cancel \; send -l -- 'f'
bind -T copy-mode-vi 'g' send -X cancel \; send -l -- 'g'
bind -T copy-mode-vi 'h' send -X cancel \; send -l -- 'h'
bind -T copy-mode-vi 'i' send -X cancel \; send -l -- 'i'
bind -T copy-mode-vi 'j' send -X cancel \; send -l -- 'j'
bind -T copy-mode-vi 'k' send -X cancel \; send -l -- 'k'
bind -T copy-mode-vi 'l' send -X cancel \; send -l -- 'l'
bind -T copy-mode-vi 'm' send -X cancel \; send -l -- 'm'
bind -T copy-mode-vi 'n' send -X cancel \; send -l -- 'n'
bind -T copy-mode-vi 'o' send -X cancel \; send -l -- 'o'
bind -T copy-mode-vi 'p' send -X cancel \; send -l -- 'p'
bind -T copy-mode-vi 'r' send -X cancel \; send -l -- 'r'
bind -T copy-mode-vi 's' send -X cancel \; send -l -- 's'
bind -T copy-mode-vi 't' send -X cancel \; send -l -- 't'
bind -T copy-mode-vi 'u' send -X cancel \; send -l -- 'u'
bind -T copy-mode-vi 'w' send -X cancel \; send -l -- 'w'
bind -T copy-mode-vi 'x' send -X cancel \; send -l -- 'x'
bind -T copy-mode-vi 'z' send -X cancel \; send -l -- 'z'
bind -T copy-mode-vi '{' send -X cancel \; send -l -- '{'
bind -T copy-mode-vi '|' send -X cancel \; send -l -- '|'
bind -T copy-mode-vi '}' send -X cancel \; send -l -- '}'
bind -T copy-mode-vi '~' send -X cancel \; send -l -- '~'
